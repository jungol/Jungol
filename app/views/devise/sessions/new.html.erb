<div class="content-bg">
  <div id="new-session" class="main-right full">
    <div id="item" class="todos">
      <div class="alt-item-head headerfull">
        <h2>Sign in</h2>
      </div>


        <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
          <p><%= f.label :email %><br />
            <%= f.email_field :email %></p>
        
          <p><%= f.label :password %><br />
            <%= f.password_field :password %></p>
        
          <% if devise_mapping.rememberable? -%>
            <p><%= f.check_box :remember_me %> <%= f.label :remember_me %></p>
          <% end -%>
        
          <p class="sign-in-button"><%= f.submit "Sign in" %></p>
        <% end %>
        
        <span class="sign-in-links"><%= render :partial => "devise/shared/links" %></span>
        
        <div class="clearall"></div>
    </div>
  </div>
</div>

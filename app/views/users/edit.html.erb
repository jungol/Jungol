<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
  <%= render 'fields', :f => f %>
  <div>
    <%= gravatar_for @user %>
    <%= link_to "change", "http://en.gravatar.com/emails", :target => "_blank" %>
  </div>
   <div class='actions'>
    <%= f.submit "Update" %>
  </div>
<% end %>


<!DOCTYPE html>
<%= javascript_include_tag "application" %>
<%= render 'layouts/stylesheets' %>
<%= stylesheet_link_tag "application" %>
<html>
  <head>
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    <link rel="shortcut icon" href="/favicon.ico" />
    <script type="text/javascript">$(document).bind('click', function(e) { var $clicked = $(e.target); if (! $clicked.parents().hasClass("lander-login")) $("#login-window").slideUp(200); });</script>
    <script type="text/javascript"> var _gaq = _gaq || []; _gaq.push(['_setAccount', 'UA-24858383-1']); _gaq.push(['_setDomainName', '.jungolhq.com']); _gaq.push(['_trackPageview']); (function() { var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true; ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js'; var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s); })(); </script>
  </head>
  <body>
    <div class="container">
      <section>
        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, :class => "flash #{name}" if msg.is_a?(String) %>
        <% end %>
        <%= yield %>
      </section>

      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development?  %>
    </div>
  </body>
</html>

<%= form_for @updates_request, :remote => true do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= f.email_field :email,
    :class => "email",
    :placeholder => "name@emailaddress.com" %>

  <%= f.submit "Submit", :class => "land-submit" %>
<% end %>

# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://jashkenas.github.com/coffee-script/
#$ ->
#  $( ".sortable" ).sortable(
#
#    stop: (event, ui) ->
#      #alert ui.item.index()
#      #alert ui.item.children('.edit_task_desc').attr('id')
#      tasks = $('ul.sortable').children('li')
#      alert tasks.length
#      for task in tasks
#        #alert task.find('span.edit_task_desc').id
#        alert ui.item.children().id
#      #params = {todo: {id: task.attr('id'), list_order: task.index() for task in tasks}}
#
##    placeholder: "sort_placeholder"
#  )
